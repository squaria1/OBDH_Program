/**
 * \file processNav.cpp
 * \brief Subsystem process navigation requests functions
 * \author Mael Parot
 * \version 1.0
 * \date 16/02/2025
 *
 * Subsystem process navigation requests functions
 *
 */

#include "processNav.h"

/**
 * \brief function to calculate the user position
 * from the doppler shift.
 *
 * \return statusErrDef that values:
 * - noError when the function exits successfully.
 */
statusErrDef calcNavFromDopplerShift() {
    statusErrDef ret = noError;

    //TODO

	return ret;
}

/**
 * \brief function to send the user position to
 * the user.
 *
 * \return statusErrDef that values:
 * - noError when the function exits successfully.
 */
statusErrDef sendNavToUser() {
    statusErrDef ret = noError;

    //TODO

	return ret;
}
